# Configuration for https://github.com/lycheeverse/lychee
# Checks links in the built Astro site under `dist/client/` and the repo root README.md.
# Run: `just lychee` (runs `just build` first).

max_concurrency = 20
timeout = 20
max_retries = 3
