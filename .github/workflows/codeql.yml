name: CodeQL

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "public/**"
      - ".github/workflows/**"
      - "astro.config.mjs"
      - "package-lock.json"
      - "package.json"
      - "tsconfig.json"
      - "wrangler.jsonc"

concurrency:
  group: "codeql-${{ github.ref }}"
  cancel-in-progress: false

permissions: {}

jobs:
  analyze:
    name: Analyze
    permissions:
      contents: read
      security-events: write # required to upload SARIF results
    uses: starhaven-io/.github/.github/workflows/reusable-codeql.yml@3599e5b87594440da1e8100ee3d757e261297a85 # v2026.07.05.1
    with:
      languages: '["actions","javascript-typescript"]'
      runner: "ubuntu-24.04"
      timeout-minutes: 30
      build-mode: ""
      build-profile: ""
